/**
 * Enum 및 상수 타입 정의
 */

export enum UserRole {
  GUEST = 'guest',
  USER = 'user',
  ADMIN = 'admin',
}

export enum Locale {
  KO = 'ko',
  EN = 'en',
  JA = 'ja',
}

export enum Currency {
  KRW = 'KRW',
  USD = 'USD',
  JPY = 'JPY',
}

